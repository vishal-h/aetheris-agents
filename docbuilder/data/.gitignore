# Real client data — never commit; only sample_data.csv is committed (anonymised fixture)
*.csv
!sample_data.csv

# Real client templates — never commit; demo/ tenant is committed in full
templates/*/
!templates/demo/
