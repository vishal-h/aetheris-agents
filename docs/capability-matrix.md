# Aetheris-Agents — Capability Matrix

_Generated by the `agents/capability_matrix_*.exs` section agents + `scripts/assemble_matrix.py` (sprint case `capability_matrix`). Re-run to update._

---

## Payslip

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| payslip_orchestrator.exs | Payslip Orchestrator | `run_command` |
| payslip_pipeline.exs | Payslip Pipeline | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| generate_employee_payslips.py | Generate HTML, PDF, and CSV payslips for one or all employees from a payroll CSV. |
| merge_employee_payslips.py | Merge per-month payslip PDFs for one employee into a single file with optional year and date range filters. |
| payslip_compute.py | Compute payslip JSON from a payroll CSV, handling salary breakdowns, deductions, and employee grouping. |

---

## Drive

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| drive_download_orchestrator.exs | Drive Download Orchestrator | `run_command` |
| drive_orchestrator.exs | Drive Workflow Orchestrator | `run_command` |
| drive_upload_orchestrator.exs | Drive Upload Orchestrator | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| drive_download.py | Download the payroll CSV from a Google Drive folder to a local path. |
| drive_upload.py | Upload per-employee payslip PDFs and CSVs from payslip/output/ to Google Drive. |
| drive_utils.py | Shared utilities for Drive folder navigation. |

---

## Email

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| email_orchestrator.exs | Email Orchestrator | `run_command` |
| email_orchestrator2.exs | Email Orchestrator | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| email_download_template.py | Download payslip_email_template.html from the Shared Drive templates folder. |
| email_send.py | Send per-employee payslip emails with PDF attachments via SMTP. |

---

## api/tenant — TAP Protocol — Tenant Side

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| at1cmd.exs | at1cmd — TAP Tenant Dispatcher | `write_blackboard`, `send_message`, `run_command` |
| at1cmd.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd_sprint.exs | at1cmd — TAP Tenant Dispatcher (sprint) | `write_blackboard`, `send_message`, `run_command` |
| at1cmd_sprint.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd_sprint.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1qry.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |

### Scripts

| Script | Purpose |
|--------|---------|
| extract_skill_hints.py | Reads a trajectory JSON file and extracts a skill hint JSON for injection into at1cmd on subsequent runs. |
| gap_analysis.py | Analyses a TAP result packet and produces a gap report. |
| package_intent.py | Packages normalised row dicts into a TAP intent packet for the enroll_students intent. |
| parse_csv.py | Reads a student enrollment CSV and outputs a JSON array of normalised row dicts. |

---

## api/ — TAP Protocol — Gateway side

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| cot1.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| cot1_stub.exs | cot1_stub — TAP Gateway Stub | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |

### Scripts

| Script | Purpose |
|--------|---------|
| build_etl_job.py | Builds an ETL job list from a TAP enroll_students intent, mapping TAP payload fields to API PascalCase and generating deterministic UUID v5 GUIDs. |
| direct_call.py | Executes a single direct-mode API call for setup capabilities with configurable duplicate handling (return_existing_id, fail, or ignore). |
| lookup_existing.py | Searches ct-api for an existing student matching name, course, and section, providing deduplication guard during enrollment. |
| notify_at1qry.py | POSTs to the Aetheris resume endpoint to wake at1qry via webhook, with non-fatal error handling. |
| ping_ct.py | Checks connectivity to the ct-api by calling GET /api/stu/_Monitor/ping with latency measurement. |
| resolve_context.py | Resolves the execution context needed to process an intent by merging prior context with vocabulary document lookups. |
| stub_cot1.py | Generates a mock TAP result packet for a given TAP intent, marking records as deterministic or non_idempotent based on presence of dob or admissionNumber. |
| submit_to_rmq.py | Publishes an ETL job message to RabbitMQ pointing to an S3 file, extracting JOB_KEY for idempotent submissions. |
| upload_etl_to_s3.py | Uploads an ETL job list to S3 with filename convention based on environment, sequence, institution code, and academic year. |
| validate_intent.py | Validates a TAP intent packet against the vocabulary document, checking required fields, enum lookups, and conditional rules. |

---

## Provenance -- Corpus Management

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| classification_orchestrator.exs | Provenance Classification Orchestrator | `run_command`, `spawn_agent`, `wait_for_all` |
| classify_batch.exs | Provenance Classify Batch | `read_file`, `run_command` |
| migration_agent.exs | Provenance Migration Agent | `run_command` |
| scan_orchestrator.exs | Provenance Scan Orchestrator | `run_command` |
| search_agent.exs | Provenance Search Agent | MCP servers (corpus_search, lattice) |
| zip_archaeologist.exs | Provenance Zip Archaeologist | `run_command` |
| zip_orchestrator.exs | Provenance Zip Orchestrator | `run_command`, `spawn_agent`, `wait_for_all` |

### Scripts

| Script | Purpose |
|--------|---------|
| approve_classifications.py | Import a reviewed CSV and apply approve/reject decisions to DuckDB. |
| classify_documents.py | Write document classification results to the DuckDB classifications table. |
| create_test_sandbox.py | Create a test sandbox for local end-to-end testing of Provenance. |
| execute_migration.py | Copy approved files to their destination, verify SHA-256, and log results to DuckDB. |
| export_for_review.py | Export proposed/needs_review classifications to CSV for human review. |
| extract_zip.py | Extract a zip file to a staging directory and return a manifest JSON. |
| init_db.py | Initialize Provenance DuckDB schema. |
| inventory_report.py | Provenance inventory report — queries DuckDB and produces a Markdown report. |
| list_migration_queue.py | Query the migration_queue view for approved files pending migration. |
| list_pending_zips.py | List zip files pending extraction from the corpus. |
| list_unclassified.py | Query DuckDB for unclassified unique files. |
| process_zip_finds.py | Process extracted zip finds against the corpus. |
| register_eval_task.exs | Register the provenance_search eval task in the Aetheris eval store. |
| seed_search_fixture.py | Seed approved classifications into a corpus DuckDB for search validation. |
| taxonomy_session.py | Taxonomy session — interactive CLI that interviews a senior auditor and writes agents/taxonomy.md. |
| validate_search.py | Validate search_agent.exs against a set of test queries. |

---

## Docbuilder

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| context_builder.exs | Docbuilder Context Builder | `read_file`, `write_file`, `run_command` |
| docbuilder_orchestrator.exs | Docbuilder Orchestrator | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| _drive.py | Shared helper: Google Drive auth, folder navigation, download/upload (fetch_template, list_templates, upload_output). |
| _format.py | Shared display formatter for cell values: currency → $1,234.56, number → thousands-separated with no trailing zeros. Used by the text renderers (generate_docx, _table_html). |
| _table_html.py | Shared helper: HTML table markup for the PDF renderers (generate_pdf + render_template). |
| chain_docbuilder.py | Chain context_builder → orchestrator as two sequential top-level mix runs; emits the orchestrator protocol (rig-p9 — the Rig Docbuilder panel entry point). |
| compute_doc.py | Computes a structured document specification from a template and data sources with sheet rendering, aggregates, and merge ranges. |
| email_send_review.py | Sends a review email to an internal alias with generated document links and client delivery information. |
| fetch_data.py | Fetches raw data from CSV or JSON source files and outputs a normalized JSON representation. |
| fetch_template.py | Fetches a template bundle from Google Drive or falls back to local committed files. |
| generate_csv.py | Renders a document specification to CSV format with optional sheet headers. |
| generate_docx.py | Renders a document specification to DOCX format with optional base file branding preservation. |
| generate_docx_from_html.py | Convert an HTML file to DOCX via Pandoc using a Bitloka reference doc (`reference.docx`) for branding and styles (m6). |
| generate_html.py | Render a Jinja2 `.html.j2` template with a context dict to HTML; absent vars render empty (`jinja2.Undefined`), autoescaping on. Replaces `render_template.py` for new doc types (m6). |
| generate_json.py | Renders a document specification to JSON format with structured sheets and columns. |
| generate_md.py | Renders a document specification to Markdown format with properly formatted tables. |
| generate_pdf.py | Render document spec to PDF in structured or narrative mode with optional Markdown template support. Jinja2 path added in m6 (`has_jinja: true`). |
| generate_xlsx.py | Renders a document specification to Excel format with optional base file preservation and native number formatting. |
| generate_xml.py | Renders a document specification to XML format with hierarchical sheet and row elements. |
| list_templates.py | Lists the template catalogue for a tenant from Drive or local files. |
| rename_output.py | Renames generated output files to the deliverable convention using context fields. |
| render_template.py | Render Markdown narrative template with variable substitution and sheet table partials into HTML. Optional fields render as empty string when absent (m5). (Deprecated in m6 — use `generate_html.py` for new doc types; kept for backward compatibility.) |
| resolve_last_run.py | Resolve "same as last month" from the run log: bump date to month-end and increment the invoice number with FY-rolling sequence (m3). |
| run_log_writer.py | Append a run entry (tenant, doc_type, run_id, timestamp, context, outputs) to the run log; idempotent by run_id (m3). |
| upload_output.py | Uploads generated output files to a tenant's Google Drive output folder. |
| validate_fields.py | Validate and normalise a raw extracted-field JSON against the context schema; exits 1 with a structured error payload if required fields are missing or values are malformed (m4). |

---

## Cloudcost

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| cloudcost_orchestrator.exs | Cloudcost · {provider} | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| _normalized.py | Shared vocabulary for canonical resource types, states, timestamp parsing, and tag coverage across adapters and detection stages. |
| fetch_do.py | Fetch DigitalOcean cost snapshot and resource inventory via read-only API calls and emit normalized JSON. |
| fetch_aws.py | Fetch AWS cost (Cost Explorer) and full-region resource inventory via read-only API and emit normalized JSON for cost and inventory. |
| fetch_linode.py | Fetch Linode billing and resource inventory from invoice items and APIs, emit normalized JSON for cost and inventory. |
| detect_orphans.py | Apply deterministic orphan detection rules to a normalized inventory and emit candidates ranked by confidence with evidence. |
| compose_report_data.py | Merge N providers' cost, inventory, and orphan artifacts into a single report payload with service totals, month-on-month delta, tag coverage, and confidence bands. |
| render_report.py | Render report data into a self-contained HTML report with inlined CSS, no external dependencies. |
| detect_optimization_signals.py | Detect exploratory AWS optimization signals from S3 lifecycle gaps, ECR image accumulation, and unused Secrets Manager entries. |

---

## Eduloka

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| eduloka_orchestrator.exs | Eduloka Orchestrator | `run_command`, `spawn_agent`, `wait_for_all` |

### Scripts

| Script | Purpose |
|--------|---------|
| edux_record.py | EduxRecord — the eduloka silver/edux schema. |
| enrich.py | Stage-3 CLI: enrich edux JSONL (silver) to gold JSONL. |
| enrichers.py | Stage-3 enrichers: pure record → payload functions. |
| export_institute.py | Stage-4b CLI: export edux/gold JSONL as gws_cse-shaped JSONL for ct-edux ingest. |
| fetch.py | Fetches search results for a term and appends provider-native JSONL to data/raw/. |
| fetch_base.py | Stage 1 (fetch) shared bits. |
| fetch_cse.py | Stage-1 fetcher: Google Custom Search JSON API (the legacy engine). |
| fetch_dataforseo.py | Stage-1 fetcher: DataForSEO (cheapest per-query; live/advanced endpoint). |
| fetch_exa.py | Stage-1 fetcher: Exa (semantic search, returns full page text). |
| fetch_serper.py | Stage-1 fetcher: Serper (Google SERP, closest paid CSE match). |
| list_terms.py | Read data/terms.txt and return the term list as JSON. |
| map.py | Stage-2 CLI: map raw JSONL (bronze) to edux JSONL (silver). |
| mappers.py | Stage-2 (map): pure transforms from a raw fetch envelope to an EduxRecord. |
| upsert_institute.py | Stage-4 CLI: upsert edux/gold JSONL into Postgres gws_cse. |

---

## Overlap Report

### Script name overlaps

No script name overlaps found.

### Identical tool sets

| Tools | Agents |
|-------|--------|
| `run_command` | payslip_orchestrator (payslip), payslip_pipeline (payslip), drive_download_orchestrator (drive), drive_orchestrator (drive), drive_upload_orchestrator (drive), email_orchestrator (email), email_orchestrator2 (email), migration_agent (provenance), scan_orchestrator (provenance), zip_archaeologist (provenance), docbuilder_orchestrator (docbuilder), cloudcost_orchestrator (cloudcost) |
| `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` | at1cmd:cot1 — TAP Gateway (api/tenant), at1cmd:at1qry — TAP Tenant Collector (api/tenant), at1cmd_sprint:cot1 — TAP Gateway (api/tenant), at1cmd_sprint:at1qry — TAP Tenant Collector (api/tenant), at1qry (api/tenant), cot1 (api/gateway), cot1_stub (api/gateway) |
| `run_command`, `spawn_agent`, `wait_for_all` | classification_orchestrator (provenance), zip_orchestrator (provenance), eduloka_orchestrator (eduloka) |
| `write_blackboard`, `send_message`, `run_command` | at1cmd:at1cmd — TAP Tenant Dispatcher (api/tenant), at1cmd_sprint:at1cmd — TAP Tenant Dispatcher (sprint) (api/tenant) |

---

## Summary

| Use case | Agents | Scripts |
|----------|--------|---------|
| payslip | 2 | 3 |
| drive | 3 | 3 |
| email | 2 | 2 |
| api/tenant | 7 | 4 |
| api/gateway | 2 | 10 |
| provenance | 7 | 16 |
| docbuilder | 2 | 24 |
| cloudcost | 1 | 8 |
| eduloka | 1 | 14 |
| **Total** | **27** | **84** |

**Unique tools across all use cases:** `run_command`, `write_blackboard`, `send_message`, `read_blackboard`, `wait_for_event`, `spawn_agent`, `wait_for_all`, `read_file`, `MCP servers (corpus_search, lattice)`, `write_file`
