# Aetheris-Agents — Capability Matrix

_Generated by `agents/capability_matrix.exs`. Re-run to update._

---

## Payslip

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| payslip_orchestrator.exs | Payslip Orchestrator | `run_command` |
| payslip_pipeline.exs | Payslip Pipeline | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| generate_employee_payslips.py | Generate HTML, PDF, and CSV payslips for one or all employees from a payroll CSV. |
| merge_employee_payslips.py | Merge per-month payslip PDFs for one employee into a single file with optional year or date range filtering. |
| payslip_compute.py | Compute payslip JSON data from a payroll CSV, handling earnings breakdown and deductions by employee type. |

---

## Drive

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| drive_download_orchestrator.exs | Drive Download Orchestrator | `run_command` |
| drive_orchestrator.exs | Drive Workflow Orchestrator | `run_command` |
| drive_upload_orchestrator.exs | Drive Upload Orchestrator | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| drive_download.py | Download the payroll CSV from a Google Drive folder to a local path. |
| drive_upload.py | Upload per-employee payslip PDFs and CSVs from payslip/output/ to Google Drive. |
| drive_utils.py | Shared utilities for Drive folder navigation. |

---

## Email

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| email_orchestrator.exs | Email Orchestrator | `run_command` |
| email_orchestrator2.exs | Email Orchestrator | `run_command` |

### Scripts

| Script | Purpose |
|--------|---------|
| email_download_template.py | Download payslip email template from Google Drive. |
| email_send.py | Send per-employee payslip emails with PDF attachments via SMTP. |

---

## api/tenant — TAP Protocol — Tenant side

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| at1cmd.exs | at1cmd — TAP Tenant Dispatcher | `write_blackboard`, `send_message`, `run_command` |
| at1cmd.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd_sprint.exs | at1cmd — TAP Tenant Dispatcher (sprint) | `write_blackboard`, `send_message`, `run_command` |
| at1cmd_sprint.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1cmd_sprint.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| at1qry.exs | at1qry — TAP Tenant Collector | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |

### Scripts

| Script | Purpose |
|--------|---------|
| extract_skill_hints.py | Reads a trajectory JSON file and extracts a skill hint JSON for injection into at1cmd on subsequent runs. |
| gap_analysis.py | Analyses a TAP result packet and produces a gap report. |
| package_intent.py | Packages normalised row dicts into a TAP intent packet for the enroll_students intent. |
| parse_csv.py | Reads a student enrollment CSV and outputs a JSON array of normalised row dicts. |

## api/ — TAP Protocol — Gateway side

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| cot1.exs | cot1 — TAP Gateway | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |
| cot1_stub.exs | cot1_stub — TAP Gateway Stub | `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` |

### Scripts

| Script | Purpose |
|--------|---------|
| build_etl_job.py | Builds an ETL job list from a TAP enroll_students intent. |
| direct_call.py | Executes a single direct-mode API call for setup capabilities. |
| lookup_existing.py | Deduplication guard: searches ct-api for an existing student matching name + course + section. |
| notify_at1qry.py | POSTs to the Aetheris resume endpoint to wake at1qry via the webhook path. |
| ping_ct.py | Checks connectivity to the ct-api by calling GET /api/stu/_Monitor/ping. |
| resolve_context.py | Resolves the execution context needed to process an intent. |
| stub_cot1.py | Generates a mock TAP result packet for a given TAP intent JSON. |
| submit_to_rmq.py | Publishes an ETL job message to RabbitMQ pointing to an S3 file. |
| upload_etl_to_s3.py | Uploads an ETL job list to S3. |
| validate_intent.py | Validates a TAP intent packet against the vocabulary doc. |

---

## Provenance — Corpus Management

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| classification_orchestrator.exs | Provenance Classification Orchestrator | `run_command`, `spawn_agent`, `wait_for_all` |
| classify_batch.exs | Provenance Classify Batch | `read_file`, `run_command` |
| migration_agent.exs | Provenance Migration Agent | `run_command` |
| scan_orchestrator.exs | Provenance Scan Orchestrator | `run_command` |
| search_agent.exs | Provenance Search Agent | MCP servers (corpus_search, lattice) |
| zip_archaeologist.exs | Provenance Zip Archaeologist | `run_command` |
| zip_orchestrator.exs | Provenance Zip Orchestrator | `run_command`, `spawn_agent`, `wait_for_all` |

### Scripts

| Script | Purpose |
|--------|---------|
| approve_classifications.py | Import a reviewed CSV and apply approve/reject decisions to DuckDB. |
| classify_documents.py | Write document classification results to the DuckDB classifications table. |
| create_test_sandbox.py | Create a test sandbox for local end-to-end testing of Provenance. |
| execute_migration.py | Copy approved files to their destination, verify SHA-256, and log results to DuckDB. |
| export_for_review.py | Export proposed/needs_review classifications to CSV for human review. |
| extract_zip.py | Extract a zip file to a staging directory and return a manifest JSON. |
| init_db.py | Initialize Provenance DuckDB schema. |
| inventory_report.py | Provenance inventory report — queries DuckDB and produces a Markdown report. |
| list_migration_queue.py | Query the migration_queue view for approved files pending migration. |
| list_pending_zips.py | List zip files pending extraction from the corpus. |
| list_unclassified.py | Query DuckDB for unclassified unique files. |
| process_zip_finds.py | Process extracted zip finds against the corpus. |
| register_eval_task.exs | Register the provenance_search eval task in the Aetheris eval store. |
| seed_search_fixture.py | Seed approved classifications into a corpus DuckDB for search validation. |
| taxonomy_session.py | Taxonomy session — interactive CLI that interviews a senior auditor and writes agents/taxonomy.md. |
| validate_search.py | Validate search_agent.exs against a set of test queries. |

---

## Docbuilder

### Agents

| Agent file | Label | Tools |
|------------|-------|-------|
| docbuilder_orchestrator.exs | Docbuilder Orchestrator | `run_command`, `write_file` |

### Scripts

| Script | Purpose |
|--------|---------|
| fetch_data.py | Reads and parses CSV or JSON data sources into structured row-based JSON. |
| compute_doc.py | Computes a document specification from a template and source data by aggregating rows and building sheets. |
| generate_csv.py | Renders the document specification to CSV format with sheet separators. |
| generate_docx.py | Renders the document specification to DOCX format with formatted tables and headings. |
| generate_json.py | Renders the document specification to JSON format with structured sheets and rows. |
| generate_md.py | Renders the document specification to Markdown format with tables and bold formatting. |
| generate_pdf.py | Renders the document specification to PDF format with HTML intermediate and styled tables. |
| generate_xlsx.py | Renders the document specification to XLSX format with column widths, number formats, and borders. |
| generate_xml.py | Renders the document specification to XML format with hierarchical sheet and row elements. |

---

## Overlap Report

### Script name overlaps

No script name overlaps found.

### Identical tool sets

| Tools | Agents |
|-------|--------|
| `read_blackboard`, `write_blackboard`, `send_message`, `run_command`, `wait_for_event` | cot1 (api/tenant), cot1 (api/gateway), cot1_stub (api/gateway), at1qry (api/tenant, at1cmd.exs), at1qry (api/tenant, at1cmd_sprint.exs), at1qry (api/tenant, at1qry.exs) |
| `run_command` | Payslip Orchestrator, Payslip Pipeline, Drive Download Orchestrator, Drive Workflow Orchestrator, Drive Upload Orchestrator, Email Orchestrator, Email Orchestrator 2, Provenance Migration Agent, Provenance Scan Orchestrator, Provenance Zip Archaeologist |
| `run_command`, `spawn_agent`, `wait_for_all` | Provenance Classification Orchestrator, Provenance Zip Orchestrator |

---

## Summary

| Use case | Agents | Scripts |
|----------|--------|---------|
| payslip | 2 | 3 |
| drive | 3 | 3 |
| email | 2 | 2 |
| api/tenant | 7 | 4 |
| api/gateway | 2 | 10 |
| provenance | 7 | 16 |
| docbuilder | 1 | 9 |
| **Total** | **24** | **47** |

**Unique tools across all use cases:** `run_command`, `write_blackboard`, `send_message`, `read_blackboard`, `wait_for_event`, `spawn_agent`, `wait_for_all`, `read_file`, `write_file`, MCP servers (corpus_search, lattice)
