# BL-254 — three retained-manifest-header sentences point "after the table" / "below" at blocks BL-253 moved out

**The defect.** BL-253 kept the manifest header verbatim, because the whole-corpus identity
assertion rebuilds the original manifest from the retained header plus the moved ranges and any
edit to the header would break it. Three header sentences point at blocks by their position in
the file. Those blocks now live in `docs/export-boundaries/`, so each pointer resolves only
through the manifest's two new index sections, `## Inclusion rulings` and
`## Export boundary log`.

**The three pointers, as of `98a8d1c`** (`git show 98a8d1c:docs/project-knowledge-manifest.md`):
- `:25` — *"the dated **THE SURFACE COLUMN** block after the table"*. The block is now in
  `docs/export-boundaries/rulings.md`.
- `:71–72` — *"recorded at the end of the 2026-09-07 boundary record below"*. The record is now in
  `docs/export-boundaries/2026-09-07.md`.
- `:77` — *"The dated block at the end of the 2026-09-07 boundary record says what changed"*. The
  record is now in the same file.

The row's done-when cites `:71` and `:76`, which are the line numbers at `1592c95`. BL-253's
budget edit (`3aa0ffe`) added one header line above the last two, shifting them. The done-when
already asks for re-derivation at the fixing commit.

`Source: filed 2026-09-15 by claude-code at agents 98a8d1c, from the flagged observation in
BL-253's review packet (packet-bl-253.md, session scratchpad): "Three sentences in the retained
manifest header still point at blocks 'after the table' or 'below' … They were kept verbatim so
the identity check would hold, so those blocks are now reachable only through the new index
lines." The arbiter deferred the fix to the next round that edits the header.`

[Index done-when trimmed 2026-09-21 to the 240-byte convention; the full text was:]

the three pointers (manifest header :25 the SURFACE COLUMN reference, and the 2026-09-07 record references at :71 and :76 — re-derive the line numbers at the fixing commit) resolve to where the blocks now live, by naming the `## Inclusion rulings` / `## Export boundary log` index entry or `docs/export-boundaries/`, rather than to a position in the file; and a check or sweep confirms no retained-header sentence still points at a moved block.
