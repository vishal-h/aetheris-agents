# BL-259 — no global lock: nothing halts all scheduled fires and triggers at once

**The finding.** The only halt is per schedule: the due query requires `enabled = 1` (`lib/aetheris/store.ex:1982`), toggled by `schedule enable` / `disable` (`cli/commands/schedule.ex:68`, `:78`). Nothing halts every schedule and trigger at once. Harness docs searched for `lock sentinel|global lock|pause all` → 0 hits; same flags on `cron` → hits in `architecture.md`, `claude-notes.md`, `m13-persistent-agents.md`.

`Source: filed 2026-09-15 by claude-code at agents `633b4e5`, from BL-200's audit of the moadim brief's Cluster 2 (`docs/evidence/BL-200.md`, close block), item 4. Harness read at `83a79c5`; every line cited above was opened by the filing session.`
