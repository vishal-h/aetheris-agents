# BL-259 — no global lock: nothing halts all scheduled fires and triggers at once

**The finding.** The only halt is per schedule: the due query requires `enabled = 1` (`lib/aetheris/store.ex:1982`), toggled by `schedule enable` / `disable` (`cli/commands/schedule.ex:68`, `:78`). Nothing halts every schedule and trigger at once. Harness docs searched for `lock sentinel|global lock|pause all` → 0 hits; same flags on `cron` → hits in `architecture.md`, `claude-notes.md`, `m13-persistent-agents.md`.

`Source: filed 2026-09-15 by claude-code at agents `633b4e5`, from BL-200's audit of the moadim brief's Cluster 2 (`docs/evidence/BL-200.md`, close block), item 4. Harness read at `83a79c5`; every line cited above was opened by the filing session.`

> **[Closed 2026-09-16: `state: done`, disposition `fixed`.]** Harness fix commit `92e9afa`, which **establishes INV-1** — the store-backed global pause — at the chokepoint BL-257 built at `65bec46`. Both rows are T1 of `m-scheduler-hardening`.
> - **One sentinel, read as a refusal reason.** `Aetheris.Admission.decide/1` returns `{:refuse, :global_pause}` for every gated origin, **checked before the cap**, so a harness that is both paused and full reports being paused. It is not a second guard: the same `decide/1` that carries `:cap_full` carries this, and `refusal_reasons/0` is now `[:global_pause, :cap_full]`. The ordering test carries a `:cap_full` control, so the claim is falsifiable rather than asserted.
> - **Store-backed.** A `harness_flags` row (`Admission.pause/0`, `unpause/0`, `paused?/0`), not process state. This is the correction the row exists for: a pause that evaporates on restart is unsafe for the same reason INV-2's count is.
> - **Every start path honors it** because they all route through `start_run/1`. Tested through the chokepoint, through `Aetheris.start_run/1` — where the assertion is that **no run was started at all**, `status/1` returning `:not_found`, not merely that an error came back — and through `Aetheris.trigger_scheduled_run/1`.
> - **The due query honors it.** `Store.do_list_due_scheduled_runs/2` returns `{:ok, []}` while paused, reading the key from `Admission.pause_flag/0` so `"global_pause"` is spelled once. Its test carries a positive control that the row is genuinely due, so the empty result under pause means the pause and not a query that finds nothing either way.
> - **Boot resume: restored, not advanced.** Under pause, `Application.do_resume_checkpoint/2` starts the run's tree — so it exists and counts toward the live total — skips `resume_from_checkpoint/1`, and leaves it `:idle`. Nothing polls for these, so `unpause/0` releases them and returns `{:ok, resumed}`. Tested end to end: seed a checkpoint, pause, restart, assert `:idle`, unpause, assert it advances.
> - **Eval is exempt** from the pause as from the cap, per the origin table.
> - **Surfaces.** CLI `aetheris pause` / `unpause` / `pause status`; API `GET|POST|DELETE /api/pause`, the two mutating verbs auth-gated. A test asserts an unauthenticated `POST` does not pause and an unauthenticated `DELETE` does not unpause a paused harness. Another asserts the global pause **disables no schedule** — it is distinct from `schedule enable|disable` (`cli/commands/schedule.ex:68`, `:78`), the per-schedule toggle this row's finding named.
> - **Fixture.** `Aetheris.Test.Admission.with_pause/1` and `paused?/0`, beside `with_cap/1`, is the surface later scheduler rows extend.
> - **Red-by-mutation, re-measured against the final test text** (the tests were rewritten mid-round, see below, and figures taken against superseded tests describe a tree that is not shipped). Over `admission_test.exs` + `admission_pause_test.exs`, 22 tests at seed 0: the pause arm made dead → **7 failures**; the due-query arm → **1 failure**; and the BL-257 mutations re-run here, cap `>=`→`>` → **3 failures**, the registry derivation → `0` → **10 failures**. Controls green (22/0, and 30/0 over four files). Every restore came from a working-copy backup and was verified by sha (`a8a1b380a4a12c0d`, `e029065655fc6d69`).
> - **The mutation that carries the store-backed claim, and the one that did not work.** The first attempt routed `paused?/0` through `GenServer.call(__MODULE__, ...)`; `decide/1` calls it from inside `Admission`'s own `handle_call`, so the mutant called itself, timed out and killed the process — 8 of 10 tests red for a reason unrelated to store-backing. **Discarded as inconclusive rather than counted.** The replacement puts the sentinel in an ETS table owned by the `Admission` process, correct within one boot and destroyed when that process dies: it reds the two restart-survival tests and the due query, and nothing else.
> - **A defect found in this round's own tests.** The cap tests did arithmetic against the ambient live count, which is global and order-dependent — a leftover run finishing mid-test drops it below the cap and admits a run the test expects refused. It passed twelve seeds and failed on the thirteenth (seed 31337), surfacing as a red control after a sha-verified restore. Rewritten to occupy first and pin the cap to runs the test holds, asserting `>=` over its own runs; twelve seeds including 31337 green. This amends BL-257's test file, in this commit.
> - **Harness gates at `92e9afa`.** `mix compile --warnings-as-errors`, `mix format --check-formatted`, `mix credo --strict`, `mix dialyzer` (`Total errors: 0`) all exit 0; `mix test` twice per BL-251: **1263 tests, 0 failures, 95 excluded** both runs. An earlier run of the same set was **credo-red** on one alias-ordering issue in `application.ex` (`exit=4`); it was fixed and the whole set re-run rather than the previous green carried. The BL-054 flake did not fire.
