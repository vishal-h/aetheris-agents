# BL-271 — a terminal worker `:DOWN` never notifies waiters or the agent tree

**The finding.** Harness at `72b7738`: the worker `:DOWN` path now durably writes `failed` (BL-267; `lib/aetheris/agent/server.ex:566-582`, `worker_down_state/2` `:971-986`), but it does not call `Coordinator.agent_failed`, `WaitRegistry.notify({:agent_done, run_id}, …)` or `maybe_update_tree_status`, all of which the `{:run_failed, _}` cast does (`:546-552`). A later `{:run_complete, :done}` cast is dropped by the `:failed` guard (`:492-495`). An `agent_done` waiter's pre-check and re-check read the child's `AgentTree` status, which this path leaves unset (`lib/aetheris/execution/loop.ex:732`, `:757`, `fetch_agent_done_payload/1` `:862-873`). Its monitor watches the child's Server, which a worker `:DOWN` does not stop (`:925-930`). So a parent blocked on `agent_done` for a child whose worker died is not woken by that transition. It resumes only on its own `timeout_ms`, or if the child's loop later fails and casts `{:run_failed, _}`, which is unguarded on `:failed` (`server.ex:533`). Inferred from the code, not demonstrated live.

**Related.** BL-268 owns the wider "terminal transitions are not final" class. This row is the missing notifications only.

`Source: filed 2026-09-18 by claude-code at agents 555ca04, from the BL-267 round's flagged observation. Harness read at 72b7738; every line cited above was opened by the filing session.`
