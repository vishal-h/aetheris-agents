# BL-273 — stale `server.ex:` line citations across Rig source, one of them a false claim

**The finding.** At agents `a137a09`, every `server.ex:<n>` citation in `rig/src` and `rig/src-tauri/src` points at an unrelated or blank line of harness `lib/aetheris/agent/server.ex` at `2d93851`. The census is 17 lines across 9 files, from `git grep -n 'server\.ex:' a137a09 -- rig/src rig/src-tauri/src`:

| Rig line at `a137a09` | cites | harness `server.ex` at `2d93851` holds |
|---|---|---|
| `rig/src-tauri/src/commands/fork.rs:47` | `:758` | a `pre_tools` error map; `encode_config` is `:1011-1017` |
| `rig/src-tauri/src/commands/harness.rs:158` | `:758` `Map.delete(:label)` | same; the `Map.delete(:label)` is `:1017` |
| `rig/src/components/modules/harness/RunList.tsx:604` | `:70-72` | `state()` typespec fields; `run/1` is `:98`, `handle_call(:run, …)` `:254` |
| `rig/src/components/modules/harness/RunList.tsx:606` | `:229-235` | the callbacks banner and `init/1` |
| `rig/src/components/modules/harness/TrajectoryView.tsx:364` | `:680` | blank |
| `rig/src/components/modules/harness/TrajectoryView.tsx:365` | `:456` | blank |
| `rig/src/components/modules/harness/TrajectoryView.tsx:504` | `:720` | blank; `maybe_add_fork_meta` is `:804-806` |
| `rig/src/hooks/types.ts:224` | `:668,939` | `message_id: message_id,` and `)` |
| `rig/src/hooks/types.ts:229` | `:720` `maybe_add_fork_meta` | blank |
| `rig/src/hooks/types.ts:231` | `:717` | the `run_loop_with_pre_tools` call |
| `rig/src/hooks/useHarness.ts:152` | `:680` | blank |
| `rig/src/hooks/useHarness.ts:153` | `:456-465` | `handle_cast(:clear_wait_condition, …)` and a comment |
| `rig/src/hooks/useTrajectory.ts:35` | `:680` | blank |
| `rig/src/lib/reconstructTrajectory.ts:90` | `:720` | blank |
| `rig/src/lib/reconstructTrajectory.ts:91` | `:759-768` | the `pre_tools` error path; `encode_config` is `:1011-1017` |
| `rig/src/lib/runSummary.ts:16` | `:70-72` | `state()` typespec fields |
| `rig/src/lib/runSummary.ts:18` | `:229-235` | the callbacks banner and `init/1` |

**The false claim.** `rig/src/hooks/useTrajectory.ts:35-36` says a failed write leaves the run without a file because "`server.ex:680` discards the write result". At `2d93851` the write result is not discarded: `execute_run/6` passes it to `run_outcome(result, write_result)` (`server.ex:741-745`), and `run_outcome(:ok, {:error, reason})` returns `{:error, {:trajectory_write_failed, reason}}` (`:1221-1223`), so the run is cast `{:run_failed, _}`.

**Already fixed.** The four lines at `TrajectoryView.tsx:364-365` and `useHarness.ts:152-153` are BL-266 T3.9's and are rewritten in the T3 apply, cited by anchor. This row is the other 13.

`Source: filed 2026-09-19 by claude-code at the BL-266 T3 apply, from the T3 plan's O6. Every Rig line above was opened at agents a137a09 and every harness line at 2d93851 by the filing session.`

[Index done-when trimmed 2026-09-21 to the 240-byte convention; the full text was:]

_(proposed)_ every `server.ex:` citation in `rig/src` and `rig/src-tauri/src` names its anchor with the line as a parenthetical and resolves at the harness commit it cites; `useTrajectory.ts`'s write-result sentence states what `run_outcome/2` does.
