# BL-274 — five of BL-266 T3's click-through checks were never exercised

**The finding.** BL-266 T3's click-through merge gate is partial. What it did reach, on 2026-09-20
against main at agents `4a43ee9` / harness `2d93851`, is the reconstructed-from-events path served
by the stream and the `client_error` path on a wrong `AETHERIS_API_TOKEN`
(`docs/milestones/bl-266-t3-implementation-notes.md` §Click-through). Five checks it did not reach:
live append on a running run, reconnect with cursor resumption, `auth_revoked` mid-stream, slot
release on navigate-away, and the poll fallback with both env vars unset.

**Why they were not run.** Every run available on this machine completes in seconds, so there is no
window in which a run is live and still appending, and no slow-provider fixture exists to create
one. The first four checks all need that window. The fifth needs a Rig build launched with both env
vars absent, which the click-through session did not have.

`Source: filed 2026-09-20 by claude-code at the BL-266 close, on agents 4a43ee9. Both halves — what
was exercised and what was not — are the T3 notes' §Click-through, written in the same round.`

`[Amended 2026-09-20. The done-when as filed read "…or an automated substitute exists", and
`test/aetheris/api/event_stream_test.exs` already holds three: `T1-5` (live append, ends
`run_terminal`), `T1-8` (`auth_revoked`, slot released) and `T1-9` (disconnect, slot released),
all against a real Bandit listener over real TCP. Read literally, they discharge three of the
five, which is not what the row means. All three stand the run up with `live_run/1` —
`Store.upsert_run(status: "running")` plus a `FakeServer` — so there is no agent loop, no
`Trajectory.Log` and no real terminal transition: the simulation verifies the simulation, which
is the class `aetheris-agents/CLAUDE.md` §Learning — BL-007 (t4 r3–r6) names. The row now
requires a substitute that exercises the real run path. **The defect is in the filing prompt's
wording, not in the filing**, and the row stays open: this round builds the instrument
(`aetheris/docs/aetheris/runbook-stream-checks.md`) and runs none of the checks.]`
