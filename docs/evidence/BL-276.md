# BL-276 — harness runbook entry for run event streaming

**What was owed.** `docs/methodology/milestone-methodology.md`'s runbook-update rule: a ticket
introducing a new configuration key, startup step or operational procedure owes a runbook entry.
BL-266 introduced four config keys and a new operational surface and the rule was missed at its
close. At `2d93851` `aetheris/docs/aetheris/runbook.md` carried no occurrence of
`max_concurrent_streams`, `stream_heartbeat_ms` or `AETHERIS_PLAYGROUND_TOKENS`.

**What landed.** Harness `179e7c5`, a 98-line pure insertion into
`docs/aetheris/runbook.md` after `## HTTP API server`, no other file touched:
`## Playground API` (namespace, bearer-auth scope, `api_bind` default) and
`## Run event streaming (BL-266)` (endpoints, node locality, the four config keys, the token
recipe below, a `curl -N` example, reverse-proxy requirements, the 503 `over_capacity` refusal).
Wire format is pointed at — `playground-api.md` §3.5/§3.6 — never restated.

**Scout step 4 — the token recipe, run rather than assumed.** Three arms, same binary, same
endpoint `GET /api/playground/policy`, same configured token:

| Arm | Start command | Token presented | Status |
|---|---|---|---|
| A — the documented path | `AETHERIS_PLAYGROUND_TOKENS=T mix aetheris server --port 4099` | `T` | **401** `unauthorized` |
| B — the working path | `AETHERIS_PLAYGROUND_TOKENS=T mix do app.config + aetheris server --port 4098` | `T` | **200**, policy body |
| C — control on B's path | as B | wrong token | **401** `unauthorized` |

C is why B's 200 is attributable to the token rather than to that path skipping auth.
A reproduces BL-275: `lib/mix/tasks/aetheris.ex` is a plain `use Mix.Task` with no
`@requirements`, so `config/runtime.exs:16` — the only reader of the variable — is never
evaluated. `lib/aetheris/api/auth_plug.ex:93` reads `Application.get_env` per call, so any start
that evaluates `runtime.exs` into application env before the request works. BL-275 stays open;
the runbook documents B and names A as the trap.

**Scope amendment.** The section as first scoped would have said "the same bearer auth as the
rest of `/api/playground/*`" in a file that never introduces the namespace, bearer auth or
`api_bind`. The arbiter widened BL-276 to carry the minimum preamble that makes it stand up,
and the done-when with it.

**Bounded out, and still owed by p2 t8.** From t8's scope
(`aetheris/docs/aetheris/milestones/m-playground-p2.md:358`): the §7 trust model and
`run_command` env hygiene, the note that open `GET /api/runs/*` leak run labels and timing once
`api_bind` is opened, overlay/sandbox retention (issue #84), and the Rig-side setup. None of
that is written here. t8's `Touches` names `docs/rig/specs.md` and `docs/rig/runbook.md` — the
Rig documents, not this file — so the harness runbook was never t8's target and this row does
not discharge t8.

**Deviations.**
1. The prompt attributed the `conn.remote_ip` / `X-Forwarded-For` caveat to the BL-266 T2
   review. It is finding 3 of the **m-playground-p2 t2 (auth plug)** review, carried by its own
   text to t8. The runbook cites it that way.
2. The prompt placed the BL-276 row in `docs/backlog-2026-06.md`. `state: done` is terminal
   (`scripts/backlog_status.py:141`, `INDEX_TERMINAL`), so a row carrying it must sit in
   `docs/backlog-2026-06-closed.md` or `--check` fails on the side-of-split arm. Filed there.
3. `docs/aetheris/runbook.md` carries manifest row 113 (`aetheris`, pinned `7257ca3`), contrary
   to the prompt's premise that it had none. Not re-pinned — that is the export boundary's job.
4. The streaming section is ~70 lines against a ~40–60 target. Every line is a fact the ticket
   named; tables and fenced examples account for the excess.
5. **The WARN prediction was wrong twice; the actual set is one.** Predicted two at scout
   (`docs/backlog-2026-06.md`, `docs/aetheris/runbook.md` row 113), then revised to three
   mid-round on finding that `docs/backlog-2026-06-closed.md` carries row 104. Actual:
   `docs/backlog-2026-06.md` alone, strict-exempt, zero FAIL. Cause —
   `scripts/drift_check.py:681` skips every row whose `surface` column is `on-demand`
   ("served from HEAD, never from the store"), comparing only `export` rows; rows 104 and 113
   are both `on-demand`. Both predictions were derived from the manifest's path column and the
   staleness rule in prose, against an instruction to derive them from `drift_check.py` itself.
   Matching manifest rows by path and never parsing the `surface` field is the substring-versus-
   field carrier of Silent-wrong-answer (harness `CLAUDE.md`), and reading the check would have
   caught it at scout.

`Source: BL-276, 2026-09-20. Harness `179e7c5` on `2d93851`; agents baseline `bef509b`. Arms A/B/C
run by claude-code after the operator stopped the IEx session that had been holding
`priv/worker/aetheris_worker` (BL-277).`
