# BL-278 — the kernel ceiling is reached by manifest-row growth alone, and the ceiling is DECREASE-ONLY

**Measured at agents `5a64de3` / harness `179e7c5`, 2026-09-20, before that date's export boundary
was written.** `python3 scripts/drift_check.py --check kernel_budget`:

```
[PASS] kernel_budget: kernel 244,750 bytes over 10 rows ≤ ceiling 250,000;
       121,870 bytes above the 122,880-byte design target
```

| figure | bytes | command |
|---|---|---|
| kernel sum, ten rows | 244,750 | `python3 scripts/drift_check.py --check kernel_budget` |
| ceiling (DECREASE-ONLY) | 250,000 | manifest header, §Kernel budget |
| headroom | 5,250 | the two above |
| `project-knowledge-manifest.md`, committed | 20,138 | `git cat-file -s HEAD:docs/project-knowledge-manifest.md` |

The manifest is itself a kernel row, so every boundary that rewrites it moves the kernel sum by
its own bookkeeping — the table grows by a row per add, the `## Inclusion rulings` list by a line
per ruling, the `## Export boundary log` by a line per boundary, and none of the three has a
removal rule.

**Per-boundary growth of the kernel sum: NOT derivable from the recorded sums.** They are not a
growth series. In chronological order, with each figure's source:

| figure | at | recorded in |
|---|---|---|
| 243,025 | agents `ea915cf` / harness `83a79c5`, 2026-09-15 | manifest header, §Kernel budget |
| 241,995 | agents `a595d73`, 2026-09-15 | manifest header, §Kernel budget |
| 241,086 | the 2026-09-15 second boundary | `docs/export-boundaries/2026-09-15.md` |
| 239,911 | agents `4fd59b6` / harness `191970a`, 2026-09-17 | `docs/export-boundaries/2026-09-17.md` |
| 244,750 | agents `5a64de3` / harness `179e7c5`, 2026-09-20 | this file |

Three falls, then one rise of 4,839. The sum is dominated by `backlog-2026-06.md`, which shrinks
when rows close and grows when they are filed: the 2026-09-17 record attributes its own −1,460
(241,371 → 239,911) to that row alone. An average over these figures would be an average over a
series whose movement is mostly a different document's.

**Per-boundary growth of the MANIFEST ROW: one interval, and it is 234 bytes.** The row's own size
is a series, but only one post-split interval exists. BL-253 (`66ef775`, 2026-09-15) moved the
inclusion rulings and the boundary-record bodies out of the manifest, so no size before that commit
is comparable to one after it. `git cat-file -s <commit>:docs/project-knowledge-manifest.md`:

| commit | date | bytes | what it was |
|---|---|---|---|
| `66ef775` | 2026-09-15 | 11,510 | the BL-253 split — the rulings leave |
| `ea915cf` | 2026-09-15 | 18,975 | the rulings index and boundary log arrive |
| `3aa0ffe` | 2026-09-15 | 19,081 | kernel budget restated |
| `527f1d1` | 2026-09-15 | 19,619 | BL-201 ceiling paragraph |
| `40bb26c` | 2026-09-15 | 19,904 | **BL-002 boundary** |
| `84e08d0` | 2026-09-17 | 20,138 | **BL-002 boundary** |

`40bb26c` → `84e08d0` is **+234 bytes across one boundary**. With n=1 that figure is the interval,
not an average, and it is stated as such. At that rate the 5,250 bytes of headroom is 22 boundaries.
The next interval is measurable at the 2026-09-20 boundary, whose record carries the post-commit sum.

**A correction to the observation that opened this row.** The Step 0 report of 2026-09-20 said
*"at this rate the ceiling is reached in a handful of boundaries by manifest growth alone"*. No rate
had been measured when that was written. The one measured interval is +234 bytes, which is ~22
boundaries, not a handful. The row's ground is the structure — a bound crossed by bookkeeping, on a
ceiling that may only be lowered — and not the rate; the rate is recorded here so a later reader
measures it rather than inheriting the impression.

**Filed, not fixed.** Nothing about the ceiling, the manifest or `kernel_budget` was changed.

`Source: filed 2026-09-20 by claude-code at the BL-002 export boundary of that date, on the
arbiter's ruling of the same date. Every byte figure above was read by the command named beside it,
at agents 5a64de3 / harness 179e7c5, before the boundary's own commits.`
