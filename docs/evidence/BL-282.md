# BL-282 — `run_command` confinement: `pivot_root` in the worker's mount namespace plus a shared confinement crate

Filed 2026-09-25 from BL-207's ruling (a); BL-207's evidence file carries the finding and the measurements at harness `2845995`.

Open for the taking round: `python3`, `mix` and `node` resolve from the host PATH today (`runner.rs:102`) and will not resolve after `pivot_root` unless bind-mounted into the new root; the round decides bind-mount versus dropping them from `PERMITTED_COMMANDS`. 24 of 36 tracked agent `.exs` files use `run_command` (agents `f57151b`).
