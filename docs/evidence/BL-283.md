# BL-283 — the harness runbook does not point at the Rig-side playground setup

Filed 2026-09-25 from BL-276's residual after m-playground-p2 t8 (closed 2026-06-13) was found not to own it; t8's scope is `aetheris/docs/aetheris/milestones/m-playground-p2.md:358`.

At harness `cf90264`, neither `docs/aetheris/runbook.md` nor `docs/aetheris/playground-api.md` points at `docs/rig/runbook.md` (`git grep -n -i "rig/runbook"` exits 1 on both); `runbook.md:1097` names Rig's `AETHERIS_API_TOKEN` inline, and `:804` points at the Rig runbook for the "stalled?" badge only.
