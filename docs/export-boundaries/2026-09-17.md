# Project-knowledge manifest — export-boundary records, 2026-09-17

**Export boundary — 2026-09-17, the m-scheduler-hardening close.** Five rows re-pinned, one of
them kernel; the kernel set is unchanged at ten names and no row changed surface:

| row | repo | surface | was | now | last changed |
|---|---|---|---|---|---|
| `rig--specs.md` | aetheris-agents | on-demand | `95b1161` | `d648d7b` | 2026-09-16 |
| `backlog-2026-06.md` | aetheris-agents | export | `c2b57f2` | `4fd59b6` | 2026-09-17 |
| `backlog-2026-06-closed.md` | aetheris-agents | on-demand | `c2b57f2` | `4fd59b6` | 2026-09-17 |
| `aetheris--CLAUDE.md` | aetheris | on-demand | `2845995` | `b079130` | 2026-09-17 |
| `aetheris--runbook.md` | aetheris | on-demand | `2bb9ff7` | `7257ca3` | 2026-09-16 |

**What moved.** Each row's commits are `git log --oneline <was>..<now> -- <path>` in its own repo.
- `backlog-2026-06.md` — BL-256..BL-262 committed and corrected (`c32d50a`, `b0d2502`), BL-264
  filed (`d648d7b`), and BL-256..BL-262 closed out of the open file. The only kernel mover.
- `backlog-2026-06-closed.md` — the same seven closes; a pin on the map only.
- `rig--specs.md` — three §6 event rows: `schedule_skipped` (BL-256, `57f6ec6`),
  `schedule_missed_fires` (BL-260, `6d965b5`), `run_timed_out` (BL-261a, `d648d7b`).
- `aetheris--CLAUDE.md` — BL-262 (`b079130`), critical rule 15's `from_map/3` composition.
- `aetheris--runbook.md` — BL-261b (`7257ca3`), run-artifact retention.

**Off the store, by rule.** The four `on-demand` movers are re-pinned and not uploaded.
`docs/aetheris/milestones/m-scheduler-hardening.md` (harness) carries **no row**, as no harness
`docs/aetheris/milestones/` document does; this boundary adds none and makes no inclusion ruling.
*Remove-all* is scoped to the manifest set (**BL-143**); `claude/` is untouched.

> **Step 0 — both halves PASS.** **0a:** `gen_index.py ../aetheris/docs/aetheris/research --check`
> exit 0 (`index.md is current (42 lines)`); `drift_check.py --check index_integrity` exit 0 (`28
> index entries ↔ 28 files at HEAD`). Mirror pair `diff -q` exit 0, both sha256 `16432ded5f31…`.
> **0b:** `./scripts/sprint.sh export_mechanism`, foreground under `timeout 600`, ended by its own
> exit in 2s, exit 0, BL-077 FINAL tally 0 blocking failures; six assertions green, U2 sweep `[PASS]`
> over 19 patterns, no marker. This is **BL-161**'s branch 1.

> **The kernel budget, reported and NOT adjudicated.** `drift_check.py --check kernel_budget`,
> pre-commit at agents `4fd59b6` / harness `191970a`: `kernel 239,911 bytes over 10 rows ≤ ceiling
> 250,000; 117,031 bytes above the 122,880-byte design target`. The same ten rows sum to 241,371 at
> agents `40bb26c`; the −1,460 is `backlog-2026-06.md` alone. The ceiling is not lowered here. The
> post-commit figure is in this boundary's review packet.

> **The connector grant was NOT checked from this session.** The first fetch after upload is the
> check.

> **Done-check prediction, written before the run.** `drift_check.py --strict` with
> `AETHERIS_DB_PATH` set, post-commit: 13 checks, 0 FAIL, **WARN set EMPTY**; `project_knowledge`
> PASS over 9 kernel rows. Basis: the pre-commit `--strict` run with the re-pin in the working tree
> returned `21 PASS  0 FAIL  0 WARN  13 INFO`.

**Ordering.** One agents commit, no harness commit; the bundle is assembled after it, from `git
show HEAD:`, and the upload waits until that commit is on `origin/main`.

Previous export: **2026-09-15**, second record (four rows re-pinned).

Exported: 2026-09-17 at aetheris-agents *(the commit carrying this record)* / aetheris `191970a`.
