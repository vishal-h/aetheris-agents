# Project-knowledge manifest — export-boundary records, 2026-09-20

**Export boundary — 2026-09-20, the streaming-contract boundary.** Six rows re-pinned, one of
them kernel, and **two rows added, both `on-demand`**; the kernel set is unchanged at ten names
and no existing row changed surface:

| row | repo | surface | was | now | last changed |
|---|---|---|---|---|---|
| `rig--specs.md` | aetheris-agents | on-demand | `d648d7b` | `a660ea3` | 2026-09-19 |
| `rig--runbook.md` | aetheris-agents | on-demand | `7d6013a` | `4a08e3d` | 2026-09-19 |
| `backlog-2026-06.md` | aetheris-agents | export | `4fd59b6` | `d3f43b7` | 2026-09-20 |
| `backlog-2026-06-closed.md` | aetheris-agents | on-demand | `4fd59b6` | `5a64de3` | 2026-09-20 |
| `aetheris--CLAUDE.md` | aetheris | on-demand | `b079130` | `53951cc` | 2026-09-19 |
| `aetheris--runbook.md` | aetheris | on-demand | `7257ca3` | `179e7c5` | 2026-09-20 |

**Two rows added, both `on-demand`**, under **THE STREAMING CONTRACT AND ITS BRIEF**
(`rulings.md`, 2026-09-20):

| row | repo path | repo | commit | last changed | surface |
|---|---|---|---|---|---|
| `aetheris--playground-api.md` | `docs/aetheris/playground-api.md` | aetheris | `2d93851` | 2026-09-19 | on-demand |
| `aetheris-agents--bl-266-brief.md` | `docs/aetheris/backlog/bl-266-run-event-streaming.md` | aetheris-agents | `bef509b` | 2026-09-20 | on-demand |

Both export names were checked against the table before adoption: neither appears in it, and each
matches the form its neighbours use — `aetheris--<basename>.md` for harness documents, and
`aetheris-agents--<slug>-brief.md` for the three existing rows out of
`docs/aetheris/backlog/`. Both source commits are on `main`.

**What moved.** Each row's commits are `git log --oneline <was>..<now> -- <path>` in its own repo.
- `backlog-2026-06.md` — BL-266 filed (`2cb3fa7`), its brief and BL-267..BL-270 (`471bc7a`,
  `555ca04`), BL-271..BL-273 (`c7ff655`, `4a43ee9`), BL-266 closed with BL-274/BL-275 filed
  (`bef509b`), BL-276 closed with BL-277 filed (`5a64de3`), and BL-278 filed (`d3f43b7`). The only
  kernel mover.
- `backlog-2026-06-closed.md` — the three closes, `c7ff655` / `bef509b` / `5a64de3`; a pin on the
  map only.
- `rig--specs.md` — `555ca04` (`terminal_seq`), `4a08e3d` (SSE transport), `3eba33e` (the emitted
  stream event and envelope parity), `a660ea3` (the `useRunStream` hook).
- `rig--runbook.md` — `4a08e3d`, BL-266 T3, Rig streaming run events over SSE.
- `aetheris--CLAUDE.md` — `53951cc` alone: BL-266 T0, critical rule 12 scoped to Aetheris-owned
  GenServers. The pin this boundary replaces, `b079130`, predates that scoping.
- `aetheris--runbook.md` — `179e7c5`, BL-276, playground API and run-event-streaming operations.

**Rows closed since the previous boundary (2026-09-17):** BL-266, BL-267, BL-276. **Filed open in
the same span:** BL-274, BL-275, BL-277, and BL-278, which this boundary filed itself.

**Off the store, by rule.** Five of the six re-pinned rows and **both added rows** are `on-demand`:
re-pinned, indexed, and not uploaded. `docs/milestones/bl-266-t3-implementation-notes.md` carries
**no row** — a milestone working artifact, out by kind. *Remove-all* is scoped to the manifest set
(**BL-143**); `claude/` is untouched. The kernel set is the same ten names it was at 2026-09-17.

> **Step 0 — both halves PASS.** **0a:** `gen_index.py ../aetheris/docs/aetheris/research --check`
> exit 0 (`index.md is current (42 lines)`); `drift_check.py --check index_integrity` exit 0 (`28
> index entries ↔ 28 files at HEAD`). Mirror pair `diff -q` exit 0, both md5
> `5562de342f0bf307feb337f7f51764a6`. **0b:** `./scripts/sprint.sh export_mechanism`, foreground
> under `timeout 600`, ended by its own exit, exit 0, BL-077 FINAL tally 0 blocking failures; six
> assertions green, U2 sweep `[PASS]` over 19 patterns, no marker. Run dir
> `aetheris/sprint/20260920_122717`. This is **BL-161**'s branch 1.

> **The kernel budget, reported and NOT adjudicated — and it now has a row.**
> `drift_check.py --check kernel_budget` at agents `5a64de3` / harness `179e7c5`, before this
> boundary's two commits: `kernel 244,750 bytes over 10 rows ≤ ceiling 250,000; 121,870 bytes above
> the 122,880-byte design target`. Headroom 5,250, against a manifest row of 20,138 bytes that grows
> at every boundary — filed this boundary as **BL-278**, open, filed not fixed, with the measured
> figures in `docs/evidence/BL-278.md`. The BL-278 commit itself moved `backlog-2026-06.md` by
> +666 bytes, so the same ten rows sum to 245,416 at agents `d3f43b7`. The ceiling is not lowered
> here and is not raised here. **Post-commit prediction, written before the commit:** the nine
> non-manifest kernel rows sum to 225,278 and are untouched by this boundary, so the post-commit
> kernel is 225,278 plus this file's committed manifest size — under the ceiling unless the manifest
> exceeds 24,722 bytes. The measured figure is in this boundary's report.

> **The connector grant was NOT checked from this session.** The first fetch after upload is the
> check. Both added rows are `on-demand`, so both are reachable only through the connector.

> **Done-check prediction, written before the run.** `drift_check.py --strict` with
> `AETHERIS_DB_PATH` set, post-commit: 0 FAIL, **WARN set EMPTY**; `project_knowledge` PASS over 9
> compared kernel rows. Basis: `drift_check.py:681` `continue`s on `surface == "on-demand"` before
> any git read, so five of the six re-pins and both adds cannot WARN; the one kernel mover,
> `backlog-2026-06.md`, is re-pinned to `d3f43b7` and this commit writes no manifest-tracked file,
> so nothing re-stales it; the manifest's own row carries `_(this export)_` and is not matched by
> check 8's regex; the other eight kernel rows were found current by the by-field sweep.

**Ordering.** Two agents commits, no harness commit, in this order: BL-278 first
(`docs/backlog-2026-06.md` is manifest-tracked, so filing it after the table is regenerated would
re-stale the row this boundary clears), then this boundary. The bundle is assembled after the
second, from `git show HEAD:`, and the upload waits until that commit is on `origin/main`.

Previous export: **2026-09-17**, the m-scheduler-hardening close (five rows re-pinned).

Exported: 2026-09-20 at aetheris-agents *(the commit carrying this record)* / aetheris `179e7c5`.
