# Project-knowledge manifest — export-boundary records, 2026-09-21

**Export boundary — 2026-09-21, the ceiling ratchet moves to the boundary.** Four rows re-pinned,
one of them kernel. One row moves from `both` to `on-demand`, so **the kernel goes from ten rows to
nine**. No row is added.

| row | repo | surface | was | now | last changed |
|---|---|---|---|---|---|
| `aetheris-agents--CLAUDE.md` | aetheris-agents | on-demand | `718bbc5` | `5eef96e` | 2026-09-20 |
| `backlog-2026-06.md` | aetheris-agents | export | `d3f43b7` | `7a35ef0` | 2026-09-21 |
| `backlog-2026-06-closed.md` | aetheris-agents | on-demand | `5a64de3` | `7a35ef0` | 2026-09-21 |
| `aetheris--runbook.md` | aetheris | on-demand | `179e7c5` | `9e5384c` | 2026-09-20 |
| `aetheris--bl-008-synthesis.md` | aetheris | **both → on-demand** | `19ba133` | `19ba133` (unchanged) | 2026-09-14 |

- `7a35ef0` closes BL-255 and BL-278 and records the rulings below.
- `5eef96e` is BL-280's amend rule.
- `9e5384c` is the harness head this round starts from.

The re-pins are `repin_manifest.py`'s. The surface change is a hand edit made under R2.

**Rulings applied** (the dated blocks in `rulings.md`, each indexed in the manifest's
`## Inclusion rulings`):

- **THE SYNTHESIS DEMOTION (R2).** The synthesis leaves the kernel. This executes the deferred
  transition that ratified D-B already specified ("demote after"), whose condition was met when the
  synthesis was ratified on 2026-09-11. It makes no new surface-assignment decision, so BL-202
  requires no new probe. The synthesis stays reachable at HEAD through
  `aetheris--research-index.md`.
- **THE CEILING RATCHET MOVES TO THE BOUNDARY (R3).** At each boundary,
  `new_ceiling = min(current_ceiling, ceil_1000(measured_kernel_bytes + 10,000))`, measured after
  the boundary's manifest edits.
- **THE BACKLOG ROW IS D-B'S QUEUE (R5).** `backlog-2026-06.md` stays `export`.

The header's design-target bullet is relabelled per the arbiter's R4. R4 carries no block because
it rules on a label and changes no figure.

**The ceiling: 250,000 → 243,000 bytes.**

| figure | bytes |
|---|---|
| kernel sum | 232,202 |
| the other eight rows at agents `7a35ef0` / harness `9e5384c` | 209,537 |
| this commit's manifest | 22,665 |
| sum + 10,000 | 242,202 |
| candidate, `ceil_1000` of that | 243,000 |
| ceiling, `min(250,000, 243,000)` | **243,000** |
| headroom | 10,798 |

- The manifest states its own sum and ceiling, so the figures were solved against the file in its
  final form.
- Rounding up to 1,000 is what makes that stable.
- The candidate is below the current 250,000, so the `min` lowers the ceiling. No ruling is needed.
- Reproduce with `python3 scripts/drift_check.py --check kernel_budget` at this commit.

The header no longer carries its older measurements. These moved here:

- 243,025 bytes at agents `ea915cf` / harness `83a79c5`, after BL-253;
- 241,995 at agents `a595d73`.

**Step 0.**

- **0a:** `gen_index.py ../aetheris/docs/aetheris/research --check` exits 0 (`index.md is current`).
  `drift_check.py --check index_integrity` PASS: 28 index entries ↔ 28 files.
- **0b:** `(cd ../aetheris && ./scripts/sprint.sh export_mechanism)` exits 0. All six assertions
  are `[OK]`, the U2 sweep is clean with no marker, and the exit contract reports 0 blocking
  failures. This is BL-161 branch 1, named here.
- **Mirror pair:** `diff -q ../aetheris/docs/methodology/triad-loop.md docs/triad-loop.md` exits 0.
  Both copies have md5 `5562de342f0bf307feb337f7f51764a6`.

**The upload route.** This boundary creates no bare name and removes one:
`aetheris--bl-008-synthesis.md`. The store route is therefore available on Step 5's terms: replace
the nine kernel documents in place, then remove the synthesis. Check 1 then compares the store
against nine names, not ten.

**Ordering.** Two agents commits and no harness commit. `7a35ef0` comes first, because the backlog
is manifest-tracked. This boundary comes second. The bundle is assembled after the second commit,
from `git show HEAD:`.

Previous export: **2026-09-20**, the streaming-contract boundary.

Exported: 2026-09-21 at aetheris-agents *(the commit carrying this record)* / aetheris `9e5384c`.

---

## Appended 2026-09-21: the upload and the connector check, REPORTED by the uploading session

**The store route completed, the three post-upload checks pass, and the connector is live.** The
claude-ui session that performed the upload reported this, and it is **not verified here**. It
cannot be: no session writing this file can see the store, and the uploading session holds the
evidence.

**The route.** The **store route**. The nine kernel documents were replaced in place, the manifest
last. `aetheris--bl-008-synthesis.md` was then removed, after the replacements rather than before,
which is Step 5's order. Each staged copy matched the bundle's sha256 and byte count, 232,202 bytes
in total. No bare name was created, so the store route was available on Step 5's terms.

**The three checks**, in the procedure's own order:

| check | result | what was read |
|---|---|---|
| 1 — count and names, over the manifest namespace | **PASS** | the store paths not under `claude/` equal the export names of the nine kernel rows, compared in both directions; `aetheris--bl-008-synthesis.md` is absent |
| 2 — content, on the movers only | **PASS** | `backlog-2026-06.md` carries BL-279 and BL-281, and BL-278 is absent from the open file. `project-knowledge-manifest.md` carries the 243,000 ceiling with the formula, the three new ruling index entries, and the 2026-09-21 log line. The other seven are byte-identical to the 2026-09-20 upload. |
| 3 — nothing predates the upload window | **PASS** | all nine were created in one 17-second window, 2026-09-21T04:47:42–04:47:59Z, with nothing outside `claude/` older; the eight `claude/` documents are enumerated and are out of the export set by construction |

**The connector was checked from the uploading session, and it is live.** `github-mcp` served
`vishal-h/aetheris-agents`, listing `docs/export-boundaries/2026-09-21.md`, and `vishal-h/aetheris`,
listing `docs/aetheris/research/bl-008-synthesis-2026-08.md`. The second listing is R2's
reachability claim observed: the demoted synthesis can be fetched on demand.

---

## Second record of this date — the backlog-retention boundary

**Export boundary — 2026-09-21, second record: the backlog-retention boundary.** Five rows
re-pinned, three of them kernel. No row is added, removed or moved between surfaces, so no
inclusion ruling is owed.

| row | repo | surface | was | now | last changed |
|---|---|---|---|---|---|
| `aetheris-agents--CLAUDE.md` | aetheris-agents | on-demand | `5eef96e` | `0bb39e8` | 2026-09-21 |
| `backlog-2026-06.md` | aetheris-agents | export | `7a35ef0` | `e176efd` | 2026-09-21 |
| `backlog-2026-06-closed.md` | aetheris-agents | on-demand | `7a35ef0` | `f174a97` | 2026-09-21 |
| `aetheris-agents--ROADMAP.md` | aetheris-agents | export | `4339ba7` | `f174a97` | 2026-09-21 |
| `methodology--milestone-methodology.md` | aetheris | export | `58991d5` | `cf90264` | 2026-09-21 |

The re-pins are `repin_manifest.py`'s.

**The kernel: 232,202 → 225,932 bytes.** `backlog-2026-06.md`: 74,475 → 66,375.

| commit | what | kernel bytes |
|---|---|---|
| agents `fb2230c` | BL-279 closed | backlog −518 |
| agents `377f8d9` | BL-281 closed | backlog −938 |
| harness `c5d2c6f` | R44 | milestone-methodology +484 |
| harness `cf90264` | R43 per repository | milestone-methodology +536 |
| agents `d37bc77` | round types, Step 0a mirror check, cc:prompt template | 0 — no kernel row touched |
| agents `f174a97` | 26 not-stated rows resolved, five closed, six triggers replaced | backlog +1,334; ROADMAP +480 |
| agents `0bb39e8` | 59 done-whens trimmed to 240 bytes | backlog −7,939 |
| agents `e176efd` | both done-when rules enforced by `backlog_status.py --check` | backlog −39 |
| this commit | header figures, log line, re-pins | manifest +330 |

The first three net −972, reconciled in the governance round's review packet.

**The ceiling: 243,000 → 236,000 bytes (R3).**

| figure | bytes |
|---|---|
| kernel sum | 225,932 |
| the other eight rows at agents `e176efd` / harness `cf90264` | 202,937 |
| this commit's manifest | 22,995 |
| sum + 10,000 | 235,932 |
| candidate, `ceil_1000` of that | 236,000 |
| ceiling, `min(243,000, 236,000)` | **236,000** |
| headroom | 10,068 |

Solved against the manifest in its final form; reproduce with
`python3 scripts/drift_check.py --check kernel_budget` at this commit.

**Step 0.**

- **0a, mirror (first run under Step 0a):** `diff -q ../aetheris/docs/methodology/triad-loop.md
  docs/triad-loop.md` exits 0, silent. Both copies have md5 `5562de342f0bf307feb337f7f51764a6`.
- **0a, index:** `gen_index.py ../aetheris/docs/aetheris/research --check` exits 0
  (`index.md is current`). `drift_check.py --check index_integrity` PASS: 28 index entries ↔ 28
  files.
- **0b:** `(cd ../aetheris && ./scripts/sprint.sh export_mechanism)` exits 0. All six assertions
  are `[OK]`, the U2 sweep is clean with no marker, and the exit contract reports 0 blocking
  failures. This is BL-161 branch 1, named here.

**The upload route.** No bare name is created or removed, so both routes are open: the bundle
route, or the store route replacing the nine kernel documents in place.

**The connector grant was not checked by this session.**

**Ordering.** One agents commit and no harness commit. The bundle is assembled after it, from
`git show HEAD:`.

Previous export: **2026-09-21**, the ceiling-ratchet boundary, recorded above.

Exported: 2026-09-21 at aetheris-agents *(the commit carrying this record)* / aetheris `cf90264`.

---

## Appended 2026-09-21: the backlog-retention upload and the connector check, REPORTED by the uploading session

**The store route completed, the three post-upload checks pass, and the connector is live.** The
claude-ui session that performed the upload reported this, and it is **not verified here**: no
session writing this file can see the store.

**The route.** The **store route** (BL-196). No bare name was created or removed. claude-ui's
Projects tool replaced the nine kernel documents in place at their bare export names, the manifest
last, and every write returned `replaced: true`. Nothing was written under `claude/` by the upload.

**The source.** The nine files were fetched through the GitHub connector at aetheris-agents
`9feebd4` / aetheris `cf90264`, the commit SHAs it served, not from the `/tmp` bundle. Each file's
byte size and sha256 prefix matched this boundary's bundle listing before upload; the nine sum to
225,932 bytes.

**The three checks**, in the procedure's own order:

| check | result | what was read |
|---|---|---|
| 1 — count and names, over the manifest namespace | **PASS** | the store paths not under `claude/` equal the export names of the nine kernel rows, compared in both directions |
| 2 — content, on the movers only | **PASS** | `backlog-2026-06.md` carries the two new header rules, the trimmed done-whens and BL-012's corrected path. `aetheris-agents--ROADMAP.md` carries the two cloudcost §Planned entries. `methodology--milestone-methodology.md` carries R43 per repository. `project-knowledge-manifest.md` carries the 236,000 ceiling, the 225,932 sum and this boundary's log line. |
| 3 — nothing predates the upload window | **PASS** | all nine were created in one window, 2026-09-21T15:44:36–15:44:56Z by the store's `created_at` stamps, `rig--current-state-2026-06.md` first and `project-knowledge-manifest.md` last, with nothing outside `claude/` older. `claude/` is enumerated, not judged: nine documents, the eight present before this boundary plus `claude/aetheris--backlog-retention-handoff-2026-09-21.md`, written by the same session after the checks |

**The connector was checked from the uploading session, and it is live.** `github-mcp` served both
repositories during the upload. This discharges the second record's "The connector grant was not
checked by this session."
