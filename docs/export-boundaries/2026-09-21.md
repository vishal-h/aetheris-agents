# Project-knowledge manifest — export-boundary records, 2026-09-21

**Export boundary — 2026-09-21, the ceiling ratchet moves to the boundary.** Four rows re-pinned,
one of them kernel. One row moves from `both` to `on-demand`, so **the kernel goes from ten rows to
nine**. No row is added.

| row | repo | surface | was | now | last changed |
|---|---|---|---|---|---|
| `aetheris-agents--CLAUDE.md` | aetheris-agents | on-demand | `718bbc5` | `5eef96e` | 2026-09-20 |
| `backlog-2026-06.md` | aetheris-agents | export | `d3f43b7` | `7a35ef0` | 2026-09-21 |
| `backlog-2026-06-closed.md` | aetheris-agents | on-demand | `5a64de3` | `7a35ef0` | 2026-09-21 |
| `aetheris--runbook.md` | aetheris | on-demand | `179e7c5` | `9e5384c` | 2026-09-20 |
| `aetheris--bl-008-synthesis.md` | aetheris | **both → on-demand** | `19ba133` | `19ba133` (unchanged) | 2026-09-14 |

- `7a35ef0` closes BL-255 and BL-278 and records the rulings below.
- `5eef96e` is BL-280's amend rule.
- `9e5384c` is the harness head this round starts from.

The re-pins are `repin_manifest.py`'s. The surface change is a hand edit made under R2.

**Rulings applied** (the dated blocks in `rulings.md`, each indexed in the manifest's
`## Inclusion rulings`):

- **THE SYNTHESIS DEMOTION (R2).** The synthesis leaves the kernel. This executes the deferred
  transition that ratified D-B already specified ("demote after"), whose condition was met when the
  synthesis was ratified on 2026-09-11. It makes no new surface-assignment decision, so BL-202
  requires no new probe. The synthesis stays reachable at HEAD through
  `aetheris--research-index.md`.
- **THE CEILING RATCHET MOVES TO THE BOUNDARY (R3).** At each boundary,
  `new_ceiling = min(current_ceiling, ceil_1000(measured_kernel_bytes + 10,000))`, measured after
  the boundary's manifest edits.
- **THE BACKLOG ROW IS D-B'S QUEUE (R5).** `backlog-2026-06.md` stays `export`.

The header's design-target bullet is relabelled per the arbiter's R4. R4 carries no block because
it rules on a label and changes no figure.

**The ceiling: 250,000 → 243,000 bytes.**

| figure | bytes |
|---|---|
| kernel sum | 232,202 |
| the other eight rows at agents `7a35ef0` / harness `9e5384c` | 209,537 |
| this commit's manifest | 22,665 |
| sum + 10,000 | 242,202 |
| candidate, `ceil_1000` of that | 243,000 |
| ceiling, `min(250,000, 243,000)` | **243,000** |
| headroom | 10,798 |

- The manifest states its own sum and ceiling, so the figures were solved against the file in its
  final form.
- Rounding up to 1,000 is what makes that stable.
- The candidate is below the current 250,000, so the `min` lowers the ceiling. No ruling is needed.
- Reproduce with `python3 scripts/drift_check.py --check kernel_budget` at this commit.

The header no longer carries its older measurements. These moved here:

- 243,025 bytes at agents `ea915cf` / harness `83a79c5`, after BL-253;
- 241,995 at agents `a595d73`.

**Step 0.**

- **0a:** `gen_index.py ../aetheris/docs/aetheris/research --check` exits 0 (`index.md is current`).
  `drift_check.py --check index_integrity` PASS: 28 index entries ↔ 28 files.
- **0b:** `(cd ../aetheris && ./scripts/sprint.sh export_mechanism)` exits 0. All six assertions
  are `[OK]`, the U2 sweep is clean with no marker, and the exit contract reports 0 blocking
  failures. This is BL-161 branch 1, named here.
- **Mirror pair:** `diff -q ../aetheris/docs/methodology/triad-loop.md docs/triad-loop.md` exits 0.
  Both copies have md5 `5562de342f0bf307feb337f7f51764a6`.

**The upload route.** This boundary creates no bare name and removes one:
`aetheris--bl-008-synthesis.md`. The store route is therefore available on Step 5's terms: replace
the nine kernel documents in place, then remove the synthesis. Check 1 then compares the store
against nine names, not ten.

**Ordering.** Two agents commits and no harness commit. `7a35ef0` comes first, because the backlog
is manifest-tracked. This boundary comes second. The bundle is assembled after the second commit,
from `git show HEAD:`.

Previous export: **2026-09-20**, the streaming-contract boundary.

Exported: 2026-09-21 at aetheris-agents *(the commit carrying this record)* / aetheris `9e5384c`.
